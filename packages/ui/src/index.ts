// Shared UI primitives — consumed by apps/mobile (Tamagui)
// Re-export Tamagui tokens and components used across screens
export { Button, Input, Text, View, Image, Stack, XStack, YStack, Separator } from 'tamagui';
export type { ButtonProps, InputProps, TextProps } from 'tamagui';
